import React from 'react'
import { MapPin, Pocket } from 'feather-icons-react/build/IconComponents';

export default function JobPostModal({title, lastCreatedTime,img, amount, remote, estimatedTime, description, domain, location, onPocketClick}) {
  return (
    <div className='w-[50%] h-[70%] mx-auto px-7 py-6 relative flex flex-col gap-4 bg-white'>
        
    </div>
  )
}
